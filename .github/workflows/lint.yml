name: lint
on:
  push:
    branches: [master]
  pull_request:

jobs:
  lint:
    uses: KayGHD/.github/.github/workflows/python-lint.yml@v1
    with:
      python-versions: '["3.12", "3.13"]'
      enable-pyright: false
