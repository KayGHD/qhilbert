name: lint
on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    uses: KayGHD/.github/.github/workflows/python-lint.yml@v1
    with:
      python-versions: '["3.12"]'
      enable-pyright: false
