name: lint
on:
  push:
    branches: [master]
  pull_request:
  workflow_dispatch:

jobs:
  lint:
    uses: KayGHD/KayGHD/.github/workflows/python-lint.yml@v1
    with:
      python-versions: '["3.12", "3.13"]'
      enable-pyright: false